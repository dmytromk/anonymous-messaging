databaseChangeLog:
  - changeSet:
      id: 4
      author: Yaroslav Kishchuk
        - addColumn:
            tableName: users
            columns:
              - column:
                  name: password
                  type: varchar(255)
                  constraints:
                    nullable: true
              - column:
                  name: salt
                  type: varchar(255)
                  constraints:
                    nullable: true